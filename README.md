Code to create a CodaBench "scoring task" that evaluates users submissions for the IJCB 2025 Footstep Recognition Competition.

Run:

```
./make.bash
```

...to build the `task.zip` file that should be uploaded to CodaBench.g